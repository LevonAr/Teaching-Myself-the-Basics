#include <cs50.h>

//Returns true-if value is our array of "values".  false-if value is not in our array of "values".

bool search(int value, int values[], int n);

//Orders array of n values in ascending order.
void order(int values[], int n);
